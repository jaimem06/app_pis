const titulosStyle = {
    color: 'blue',
    fontWeight: 'bold',
    // Agrega más estilos aquí
};

const inputStyle = {
    width: '100%',
    padding: '12px 20px',
    margin: '8px 0',
    boxSizing: 'border-box',
    // Agrega más estilos aquí
};

const buttonsForm = {
    display: 'flex',
    justifyContent: 'space-between',
    // Agrega más estilos aquí
};

const cancelbutton = {
    backgroundColor: 'red',
    color: 'black',
    // Agrega más estilos aquí
};

const buttonADD = {
    backgroundColor: 'green',
    color: 'black',
    // Agrega más estilos aquí
};